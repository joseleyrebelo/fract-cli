#!/bin/zsh

# Excluding usual JS 'dist' folders.
# '-m1' - Represents one match per file.
grep -rnioE -m1 "@fract" --exclude-dir={'*node_modules*',build,dist,'./.*'}

# @todo - verify -rnioE

# @fract
# @casts/fixes
# - Index files and their last read time; only read again if file's
# updated time is bigger than the indexed last read time.
# - Make use of casts/fracture imagery, on tool naming and way of usage
